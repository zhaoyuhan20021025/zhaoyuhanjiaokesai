/**
 * 距离量测
 */
export class LengthMeasure extends Measure {
    constructor(engine: any, options?: {});
    /** @private */ private _editor;
    /** @private 总长度 */
    private _length;
    /** @private 分段距离 */
    private _segmentLengths;
    /** @private */ private _indexOffset;
    /** @private */
    private _infoMarkersBuffer;
    /** @private */
    private _moveMarkerBuffer;
    /** @private */
    private _totalMarkerBuffer;
    _engine: any;
    _options: {};
    /** @private */
    private initEditor;
    /** @private */
    private initInfoMarkers;
    _infoMarkers: any;
    _moveMarker: any;
    _infoMarkersDataSource: BufferDataSource | undefined;
    _moveMarkerDataSource: BufferDataSource | undefined;
    _totalMarkerDataSource: BufferDataSource | undefined;
    /**
     * 生成分段距离的标注
     * @param {number} feature 分段距离
     * @returns 分段距离的标注
     */
    createInfoMarkers(feature: number): HTMLDivElement;
    /**
     * 生成总长度的信息标注
     * @param {number} feature 总长度
     * @returns 总长度的信息标注
     */
    createTotalMarker(feature: number): HTMLDivElement;
    _node: any;
    /** @private */
    private get selectedIndex();
    /**
     * 总长度
     */
    get length(): number;
    get segmentLengths(): any[];
}
import { Measure } from "./Measure";
import { BufferDataSource } from "../../dataSource/BufferDataSource";
